
function calculateSupply(age,amount){
 lifeTimeSupply = age*amount;
 return console.log(`You will need ${lifeTimeSupply} to last you until the old age of ${age}`)
}
calculateSupply(52,2);
calculateSupply(60,3);
